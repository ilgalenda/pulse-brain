---
preset:                         # REQUIRED. The active_preset value that selects this file (kebab-case)
title:                          # REQUIRED. Human name of the role
description:                    # REQUIRED. One sentence: when this preset is the right lens
default_domains: []             # TAXONOMY domains that LEAD read-routing (biased, never exclusive)
lead_instruments: []            # instruments foregrounded for this role, by registry name. Skills/agents bare (research-agent), commands with the slash (/build)
tone:                           # one line. The role's register/emphasis (NOT a new voice)
updated:                        # YYYY-MM-DD
---

<!--
TEMPLATE. Do not treat as a live preset.
PRESET = a selectable OPERATING LENS for one of {{OWNER}}'s roles (the Persona and
Presets layer). It biases focus, default domains, tone, and which instruments lead;
it holds NO knowledge and changes NO knowledge. Switching the active preset (the
`active_preset` anchor in memory/core.md, via /preset) reconfigures how the brain
reads and prioritises; the vault, the dual-role identity, and the laws are constant.
See PERSONA-PRESETS.md for the authority: the four levers (§3), the contract (§4),
the lens-not-filter rule (§6), and the identity boundary (§7).

LENS, NOT FILTER. default_domains LEAD read-routing; they never suppress a domain
the task clearly needs. The task overrides the preset.

ONE VOICE. `tone` is register and emphasis, not a character. A preset colours the
single voice to {{OWNER}}; it never replaces it.
-->

## Focus
<!-- The role's default priorities: what the brain leads with when {{OWNER}} has not
     named a task. Seeds core.md's "Current focus" framing; never overwrites live focus. -->

## How the levers play together
<!-- One short paragraph: how default_domains, lead_instruments, and tone combine for
     this role, the working texture, refined at instance time against real use. -->
