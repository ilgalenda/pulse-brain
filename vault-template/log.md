<!--
TEMPLATE — the brain's chronological evolution log. APPEND-ONLY. The kernel adds
an entry for every meaningful change (source ingested, page created/revised,
contradiction flagged/resolved, capability added). This is the brain's changelog
AND its recall timeline (there is no separate journal). Append-only makes it
collision-safe across concurrent tabs. Newest entries at the bottom.

Entry format (one per change):

## YYYY-MM-DD HH:MM — <short title>
- **what:** what changed
- **where:** [[page]] / pillar / domain affected
- **why:** the source or trigger ([[source-note]])
- **flags:** any contradiction raised/resolved (optional)
-->

# {{OWNER}}'s Brain — Evolution Log
